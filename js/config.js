/**
 * Local storage key used for serialized storyboard state.
 * @type {string}
 */
export const STORAGE_KEY = "ai_storyboard_state_v1";

/**
 * Cache API bucket name used for keyframe image blobs.
 * @type {string}
 */
export const IMAGE_CACHE_NAME = "ai-storyboard-images-v1";

/**
 * Debounce delay (in milliseconds) for text-input persistence writes.
 * @type {number}
 */
export const SAVE_DEBOUNCE_MS = 75;
